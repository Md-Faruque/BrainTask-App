apiVersion: apps/v1
kind: Deployment
metadata:
  name: brainapp-deployment
  labels:
    app: brain-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: brain-app
  template:
    metadata:
      labels:
        app: brain-app
    spec:
      containers:
        - name: brinappc
          image: 312669614823.dkr.ecr.us-east-1.amazonaws.com/braintasks-app:latest
          ports:
            - containerPort: 80
